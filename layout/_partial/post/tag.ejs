<% if (post.tags && post.tags.length){ %>
  <div class="article-category">
    <%- list_categories(post.tags, {
      show_count: false,
      class: 'article-category',
      style: 'none',
      separator: ' '
    }) %>
  </div>
<% } %>